#!/bin/bash


# sudo apt install cloudflare-warp
# warp-cli register
# warp-cli connect


# DOWNLOAD_DEST=~/Downloads # no trailing slash
# WARP_CLIENT_PACKAGE=cloudflare_warp_2022_9_591_1_amd64_fa851354e5.deb
# WARP_CLIENT_DOWNLOAD_LINK=https://pkg.cloudflareclient.com/uploads/${WARP_CLIENT_PACKAGE}

# sudo apt-get install wget -y
# sudo rm ${DOWNLOAD_DEST}/cloudflare_warp*
# wget -P ${DOWNLOAD_DEST} ${WARP_CLIENT_PACKAGE}

# sudo su
# sudo dpkg -i ${WARP_CLIENT_PACKAGE}
# sudo apt --fix-broken install
# sudo apt update
# sudo apt upgrade
# warp-cli register
# warp-cli connect